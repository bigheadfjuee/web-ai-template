<!--
Verification for this change is manual (no automated test suite is in scope).
Each task names the observable behavior and the manual or `spectra validate`
check that confirms completion.
-->

## 1. Backend scaffold (FastAPI service managed by uv)

- [x] 1.1 Establish the FastAPI service managed by uv — initialize `backend/pyproject.toml` declaring `fastapi` and `uvicorn[standard]` as runtime dependencies and `ruff` (or equivalent linter) as a dev dependency, then run `uv lock` from `backend/` to produce `backend/uv.lock`. Verify by running `uv sync --frozen` in a clean state: the command MUST exit `0` and install the locked versions.
- [x] 1.2 Create the app factory and configuration module — implement the FastAPI app factory in `backend/app/main.py` exporting `create_app()` plus a module-level `app = create_app()`, alongside `backend/app/__init__.py` and `backend/app/config.py` (settings loader, e.g. via `pydantic-settings` or stdlib). Verify by running `uv run python -c "from app.main import app; print(type(app).__name__)"` from `backend/` and confirming the output is `FastAPI`.
- [x] 1.3 Implement the health endpoint at `GET /api/health` returning `{"status":"ok"}` with `Content-Type: application/json`. Verify by running `uv run uvicorn app.main:app --port 8000` in one shell and `curl -fsS http://localhost:8000/api/health` in another: the body MUST be exactly `{"status":"ok"}` and the status code MUST be `200`.
- [x] 1.4 Add the CORS allowlist for dev server — register FastAPI `CORSMiddleware` allowing only origin `http://localhost:5173`, no wildcard. Verify by issuing a preflight `OPTIONS /api/health` with `Origin: http://localhost:5173` and confirming the response includes `Access-Control-Allow-Origin: http://localhost:5173`; then repeat with `Origin: http://evil.example` and confirm no `Access-Control-Allow-Origin` header is returned for that origin.

## 2. Frontend scaffold (Vite + Vue 3 + TypeScript SPA)

- [x] 2.1 Initialize the Vite + Vue 3 + TypeScript SPA scaffold under `frontend/` — create `package.json` (vue 3, vite, typescript, vue-tsc), `vite.config.ts`, `tsconfig.json`, `index.html`, `src/main.ts`, and `src/App.vue` following the Vue 3 + TypeScript + `<script setup>` SFCs, no state library decision (Pinia and Vue Router are intentionally omitted). Verify by running `npm install` then `npm run build`: the build MUST exit `0` and `frontend/dist/index.html` MUST exist and reference a hashed JS bundle via `<script type="module">`.
- [x] 2.2 Implement the health-check view wired to backend — add `src/components/HealthCheck.vue` that calls `fetch('/api/health')` on mount and renders "Backend status: ok" on `200`, "Backend status: <status>" on other 2xx, or "Backend status: unreachable" on network/5xx errors; mount it inside `App.vue`. Verify by running the backend on `:8000` and the frontend dev server on `:5173`, opening `http://localhost:5173/` in a browser, and confirming the rendered text matches the live backend response; then stop the backend, reload, and confirm the page shows "Backend status: unreachable" without an uncaught console exception.
- [x] 2.3 Configure the Vite dev-server proxy to backend — in `vite.config.ts`, add a `server.proxy` entry forwarding `/api` to `http://localhost:8000`. Verify by running `npm run dev` with the backend live on `:8000` and issuing `curl -fsS http://localhost:5173/api/health`: the response MUST be the FastAPI `{"status":"ok"}` body proxied through Vite.

## 3. Container deploy (Dockerfiles, nginx, compose)

- [x] 3.1 Author the backend Dockerfile following the `uv` with `pyproject.toml` + `uv.lock`, no `requirements.txt` decision — base on a slim Python image, copy `pyproject.toml` and `uv.lock`, run `uv sync --frozen --no-dev`, copy `app/`, expose `8000`, and set `CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]`. Verify by running `docker build -f backend/Dockerfile backend/` from the repo root: the build MUST succeed; then `docker run --rm -p 8000:8000 <image>` and `curl -fsS http://localhost:8000/api/health` MUST return `{"status":"ok"}`.
- [x] 3.2 Author the per-service Dockerfiles for the frontend — write a multi-stage `frontend/Dockerfile` whose first stage runs `npm ci` and `npm run build`, and whose second stage starts from an `nginx` base image, copies `dist/` into `/usr/share/nginx/html`, and copies `nginx/nginx.conf` into the nginx config path; the existing empty top-level `Dockerfile` MUST be deleted as part of this task (per the "Per-service Dockerfiles instead of a single top-level Dockerfile" decision). Verify by running `docker build -f frontend/Dockerfile .` (or `frontend/`, matching the chosen build context) from the repo root: the build MUST succeed and the resulting image's `/usr/share/nginx/html/index.html` MUST reference the hashed JS bundle.
- [x] 3.3 Implement the nginx reverse proxy configuration following the nginx config lives in a top-level `nginx/` directory, mounted into the frontend image decision — write `nginx/nginx.conf` that serves `/usr/share/nginx/html` with an SPA fallback (`try_files $uri /index.html`) at `location /`, and reverse-proxies `location /api/` to `http://backend:8000` (the Compose service DNS name). Verify after task 3.5 by curling `http://localhost:8080/some/unknown/route` and confirming an HTTP `200` response with `index.html` content rather than `404`.
- [x] 3.4 Configure the docker-compose stack with single host port and nginx as the only host-exposed service — replace the empty top-level `docker-compose.yml` with a definition of services `frontend` (built from `frontend/Dockerfile`, `ports: ["8080:80"]`) and `backend` (built from `backend/Dockerfile`, no `ports:` entry, reachable only on the internal Compose network as `backend:8000`). Verify by running `docker compose config` and confirming no `ports` mapping exists on `backend`; then with the stack up, confirm `curl -fsS http://localhost:8000/api/health` from the host fails (connection refused / timeout) while `curl -fsS http://localhost:8080/api/health` succeeds.
- [x] 3.5 End-to-end verification — from the repo root, run `docker compose up --build` and wait for both services to report ready, then confirm: (a) `curl -fsS http://localhost:8080/` returns HTTP `200` with the built SPA `index.html`, (b) `curl -fsS http://localhost:8080/api/health` returns `{"status":"ok"}`, and (c) opening `http://localhost:8080/` in a browser renders "Backend status: ok". All three checks MUST pass; if any fails, fix the relevant config and re-run.

## 4. Documentation and repo hygiene

- [x] 4.1 Author the repo-root quickstart documentation — expand `README.md` with three runnable blocks (backend `uv sync` + `uv run uvicorn`, frontend `npm install` + `npm run dev`, container `docker compose up --build`) and explicit mention of `http://localhost:8080/api/health` as the verification URL. Verify by content review against the `Repo-root quickstart documentation` requirement: all four mandated elements MUST be present in the file.
- [x] 4.2 Add `.gitignore` entries at the repo root covering Node (`node_modules/`, `dist/`), Python (`__pycache__/`, `.venv/`, `*.pyc`), and editor/OS noise (`.DS_Store`, `.idea/`, `.vscode/`). Verify by running `git status` after a fresh `npm install` and `uv sync`: neither `frontend/node_modules/` nor `backend/.venv/` MUST appear in untracked files.
- [x] 4.3 Run `spectra validate bootstrap-vue-fastapi-stack` and confirm it reports no errors. Verify by command exit status `0`.
