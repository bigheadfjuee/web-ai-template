## 1. Backend — dependencies and database wiring

- [x] 1.1 Satisfy the SQLAlchemy and Alembic dependencies and SQLAlchemy 2 async engine and session factory requirements using the SQLAlchemy 2 async with asyncpg driver approach — add `sqlalchemy[asyncio]>=2.0`, `asyncpg`, `alembic`, and `pydantic[email]` to `backend/pyproject.toml` runtime dependencies, run `uv lock` to update `backend/uv.lock`, then create `backend/app/db.py` exporting a `create_async_engine`-based `engine`, `AsyncSessionLocal`, and the `get_db` FastAPI dependency that yields an `AsyncSession`. Verify by running `uv sync --frozen` from `backend/` (exit 0) and `uv run python -c "from app.db import get_db; print('ok')"` (prints `ok` without import error).
- [x] 1.2 Add database lifespan to app factory — update `create_app()` in `backend/app/main.py` to register a FastAPI `lifespan` context manager that creates the SQLAlchemy engine on startup and disposes it on shutdown (following the "Database lifespan in app factory" decision). The existing CORS middleware and `/api/health` route MUST remain unchanged. Verify by running `uv run uvicorn app.main:app --port 8000` and confirming `GET http://localhost:8000/api/health` still returns `200 {"status":"ok"}`.
- [x] 1.3 Add `DATABASE_URL` to config — extend `backend/app/config.py` `Settings` to include `database_url: str` read from the `APP_DATABASE_URL` environment variable (or `DATABASE_URL` as a fallback). Verify by running `APP_DATABASE_URL=postgresql+asyncpg://u:p@localhost/db uv run python -c "from app.config import get_settings; print(get_settings().database_url)"` and confirming it prints the URL.

## 2. Backend — User model and Alembic migration

- [x] 2.1 Create User ORM model — create `backend/app/models/__init__.py` and `backend/app/models/user.py` defining the SQLAlchemy `User` mapped class with columns `id UUID PK`, `username VARCHAR(128) UNIQUE NOT NULL`, `email VARCHAR(256) UNIQUE NOT NULL`, `created_at TIMESTAMPTZ DEFAULT now()`, `updated_at TIMESTAMPTZ DEFAULT now()` (following the "User model fields" decision). Verify by running `uv run python -c "from app.models.user import User; print(User.__tablename__)"` and confirming output is `users`.
- [x] 2.2 Apply the Alembic for schema migrations decision to fulfil the Alembic migration for users table requirement — run `uv run alembic init alembic` from `backend/` to produce `backend/alembic.ini` and `backend/alembic/env.py`; configure `env.py` to use the async engine from `app.db` and import `app.models` for `target_metadata`; generate migration `0001_create_users_table` that creates the `users` table in `upgrade()` and drops it in `downgrade()`. Verify by starting a local postgres instance, setting `DATABASE_URL`, running `alembic upgrade head` (exit 0, `users` table present) then `alembic downgrade -1` (table dropped).

## 3. Backend — Pydantic schemas and CRUD router

- [x] 3.1 Implement Pydantic v2 request and response schemas following the Pydantic v2 schemas split into request / response decision — create `backend/app/schemas/__init__.py` and `backend/app/schemas/user.py` exporting `UserCreate` (`username: str`, `email: EmailStr`), `UserUpdate` (`username: str | None = None`, `email: EmailStr | None = None`), and `UserResponse` (`id: UUID`, `username: str`, `email: str`, `created_at: datetime`, `updated_at: datetime`) with `model_config = ConfigDict(from_attributes=True)`. Verify by running `uv run python -c "from app.schemas.user import UserResponse; print(UserResponse.model_fields.keys())"` and confirming all five field names appear.
- [x] 3.2 Implement the CRUD router at /api/users and register it in the app factory (/api/users router registered in app factory) — create `backend/app/routers/__init__.py` and `backend/app/routers/users.py` implementing the five endpoints (`POST /api/users` → 201, `GET /api/users` → 200, `GET /api/users/{id}` → 200/404, `PUT /api/users/{id}` → 200/404/409, `DELETE /api/users/{id}` → 204/404); apply the IntegrityError mapped to 409 requirement by catching `sqlalchemy.exc.IntegrityError` and returning `409 {"detail":"Username or email already exists"}`; update `updated_at` explicitly on PUT. Register the router in `create_app()` with prefix `/api`. Verify by running the backend locally (with postgres up), then executing the full `curl` sequence from the Implementation Contract: create → 201, list → 200 array, get → 200, update → 200 with updated `updated_at`, delete → 204, second delete → 404, duplicate create → 409.

## 4. Compose — PostgreSQL service and backend wiring

- [x] 4.1 Fulfil the PostgreSQL service in docker-compose.yml, PostgreSQL 16 service in Compose, and Named volume postgres_data declared in Compose requirements — add a `postgres` service using `postgres:16-alpine`, environment variables `POSTGRES_USER=appuser / POSTGRES_PASSWORD=apppassword / POSTGRES_DB=appdb`, named volume `postgres_data:/var/lib/postgresql/data`, and healthcheck `pg_isready -U appuser -d appdb` with interval 5s / timeout 5s / retries 5 (PostgreSQL service in Compose with named volume decision); add top-level `volumes: postgres_data:` declaration; update the `backend` service to add `DATABASE_URL=postgresql+asyncpg://appuser:apppassword@postgres:5432/appdb` and `depends_on: postgres: condition: service_healthy` (Backend service depends on postgres health decision). Verify by running `docker compose config` and confirming `postgres_data` appears in the volumes section and `backend.depends_on.postgres.condition` is `service_healthy`.
- [x] 4.2 Fulfil the Automatic migration on backend container start requirement by applying the Backend Dockerfile uses migration-then-start CMD decision — change the `CMD` in `backend/Dockerfile` from `["uvicorn", ...]` to `["sh", "-c", "alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port 8000"]`. Verify by running `docker compose up --build` and confirming in the backend container logs that `alembic upgrade head` appears before any uvicorn startup lines, then `curl -fsSk https://localhost/api/users` returns `200 []`.

## 5. Frontend — API client and Users page

- [x] 5.1 Implement the API client module requirement using the API client in users.ts (typed fetch, no library) decision — create `frontend/src/api/users.ts` exporting `listUsers(skip=0, limit=100)`, `getUser(id)`, `createUser(data)`, `updateUser(id, data)`, `deleteUser(id)` as async functions wrapping `fetch`; define and throw `ApiError` with `status: number` and `message: string` on any non-2xx response. Verify by running `npm run build` from `frontend/` with the new file present: the TypeScript compiler MUST emit no errors related to `users.ts`.
- [x] 5.2 Implement UsersView component — create `frontend/src/views/UsersView.vue` using `<script setup lang="ts">` that: (a) fetches the user list on mount via `listUsers` and stores it in a `ref`; (b) displays a "No users yet" message when the list is empty; (c) renders a table/list with one row per user showing `username` and `email`; (d) includes a create form (username + email inputs, submit button) that calls `createUser`, clears inputs on success, re-fetches the list, and shows the `ApiError.message` on 409; (e) supports inline edit per row (Edit / Save / Cancel buttons) that calls `updateUser` and re-fetches on save; (f) includes a Delete button per row that calls `deleteUser` and re-fetches on 204 — all per the "Users page component", "Create user form", "Edit user inline", and "Delete user with confirmation" requirements. Verify by running `npm run build` from `frontend/`: exit 0, no TypeScript errors.
- [x] 5.3 Satisfy the Tab navigation in App.vue requirement using the frontend users page as toggled section in App.vue decision — update `frontend/src/App.vue` to import `UsersView`, add an `activeTab` ref defaulting to `"health"`, render two tab buttons ("Health" / "Users"), and conditionally show `<HealthCheck />` or `<UsersView />` based on `activeTab`. Verify by running `npm run build` from `frontend/`: exit 0. Then `docker compose up --build` and opening `https://localhost/` in a browser: both tabs visible, switching to "Users" shows the user list, creating a user via the form shows the new row.

## 6. End-to-end verification

- [x] 6.1 Full CRUD curl sequence against live stack — run `docker compose up --build` from the repo root, then execute: (a) `curl -fsSk https://localhost/api/users` → `200 []`, (b) POST alice → `201` with UUID id, (c) GET alice by id → `200`, (d) PUT alice email → `200` with `updated_at > created_at`, (e) DELETE alice → `204`, (f) GET alice → `404`, (g) POST duplicate username → `409`. All seven assertions MUST pass; if any fails, fix the root cause before marking done.
- [x] 6.2 Browser smoke test — with the stack running, open `https://localhost/` (accept self-signed cert), switch to "Users" tab, create a user, edit its email, delete it; confirm each action updates the visible list without a full page reload and no uncaught console errors appear. Also confirm the "Health" tab still shows "Backend status: ok".
