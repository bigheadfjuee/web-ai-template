## 1. Infrastructure — Docker Compose & Keycloak Service

- [x] 1.1 Add `keycloak` service to `docker-compose.yml` (Keycloak service in Compose; design: Keycloak as the sole Kerberos boundary; design: Keycloak service (`docker-compose.yml`)): image `quay.io/keycloak/keycloak:latest`, command `start-dev --import-realm`, env vars `KC_HTTP_RELATIVE_PATH=/auth`, `KC_HOSTNAME=localhost`, `KEYCLOAK_ADMIN`, `KEYCLOAK_ADMIN_PASSWORD`, mount `./keycloak/realm-export.json` into `/opt/keycloak/data/import/realm-export.json`, healthcheck on `GET http://keycloak:8080/auth/health/ready`, no host-port exposure. This is the sole Kerberos boundary — Vue and FastAPI MUST NOT import any Kerberos library. Verify: `docker compose config` shows the keycloak service with the correct mount and env; `docker compose up keycloak` starts and healthcheck passes within 60 s.

- [x] 1.2 Add `depends_on: keycloak: condition: service_healthy` to the `backend` service in `docker-compose.yml` so the backend container starts only after Keycloak is healthy. Verify: `docker compose up` on a clean environment logs that the `backend` container starts after Keycloak's healthcheck passes. Satisfies requirement: Keycloak starts before the backend.

- [x] 1.3 Add `location /auth/` proxy block to `nginx/nginx.conf` before the existing `location /` block (design: nginx `/auth/` proxy block; design: Keycloak hostname via nginx proxy, not direct port exposure): `proxy_pass http://keycloak:8080;` with `proxy_set_header Host`, `X-Real-IP`, `X-Forwarded-For`, `X-Forwarded-Proto`. Keycloak MUST NOT expose its port on the host — all external Keycloak traffic flows through nginx. Verify: `docker compose up` then `curl -sk https://localhost/auth/realms/app/.well-known/openid-configuration` returns HTTP 200 JSON with `"issuer"` key; `curl -sk https://localhost/api/health` still returns `{"status":"ok"}`. Satisfies requirements: nginx proxy block for Keycloak, Keycloak OIDC discovery endpoint is reachable through nginx.

- [x] 1.4 Confirm nginx per-service Dockerfiles remain intact (requirement: Per-service Dockerfiles): `nginx/Dockerfile` multi-stage build (Node builder + nginx runtime) MUST copy the updated `nginx/nginx.conf` containing the new `/auth/` block. Verify: `docker build -f nginx/Dockerfile .` from the repo root completes successfully and the resulting nginx image's config includes `location /auth/`.

## 2. Keycloak Realm Configuration Artifact

- [x] 2.1 Create `keycloak/realm-export.json` (design: Realm export JSON as the configuration artifact; design: Keycloak realm export (`keycloak/realm-export.json`)) defining realm `app` with: OIDC public client `vue-spa` (`publicClient: true`, redirect URI `https://localhost/*`, `pkceCodeChallengeMethod: S256`, access-token lifespan 300 s); LDAP User Federation component with placeholder fields (`connectionUrl: ldap://AD_HOST:389`, `bindDn: CN=svc-keycloak,...`, `usersDn: OU=Users,...`); Kerberos/SPNEGO browser authentication flow with forms fallback. This file is the sole source of truth for all Keycloak configuration. Verify: on first `docker compose up`, Keycloak admin console shows realm `app` with client `vue-spa` and the LDAP federation entry; `git ls-files keycloak/` shows only `realm-export.json` with no `.keytab` file. Satisfies requirements: Realm export configuration artifact, Keycloak configuration directory, No keytab files are tracked by git.

- [x] 2.2 Add `keycloak/*.keytab` to `.gitignore` to prevent accidental keytab commits. Verify: create a dummy `keycloak/test.keytab` file, run `git status`, confirm the file does not appear as untracked, then delete the dummy file. Satisfies requirement: No keytab files are tracked by git.

## 3. Frontend — keycloak-js OIDC Client (Authorization Code + PKCE)

- [x] 3.1 Add `keycloak-js` as a production dependency in `frontend/package.json` and run `npm install` to update `package-lock.json`. This enables the Authorization Code + PKCE flow (no implicit flow, no client secret) as described in design: Authorization Code + PKCE (no implicit flow, no client secret). Verify: `npm ls keycloak-js` from `frontend/` shows the installed version; `npm run build` exits with status `0`. Satisfies requirement: Vite + Vue 3 + TypeScript SPA scaffold (keycloak-js present).

- [x] 3.2 Create `frontend/src/auth.ts` (design: Vue OIDC client (`frontend/src/auth.ts`)) exporting: singleton `keycloak = new Keycloak({ url: '/auth', realm: 'app', clientId: 'vue-spa' })`; `initKeycloak(): Promise<boolean>` calling `keycloak.init({ onLoad: 'login-required', pkceMethod: 'S256' })`; `getToken(): string` returning `keycloak.token ?? ''`. Verify: `npm run build` compiles without TypeScript errors; `grep -r "Keycloak" frontend/dist/` returns at least one match (keycloak-js bundled). Satisfies requirements: Vue OIDC client module, keycloak-js is present in the production bundle.

- [x] 3.3 Update `frontend/src/main.ts` to call `initKeycloak()` before `createApp(App).mount('#app')`. If `initKeycloak()` resolves to `false` or rejects, render an error message in `#app` instead of mounting Vue. Verify: with Keycloak running, navigating to the SPA redirects to Keycloak login before the Vue app renders; if Keycloak is unreachable, the page shows an error text. Satisfies requirement: Vue app mounts only after successful authentication.

- [x] 3.4 Update the `request()` helper in `frontend/src/api/users.ts` to include `Authorization: Bearer ${getToken()}` in every outgoing fetch request. Verify: in browser DevTools, `GET /api/users` request includes `Authorization: Bearer <token>` header after login. Satisfies requirement: API requests carry a Bearer token.

## 4. Backend — Stateless JWT Validation via JWKS (No Introspection)

- [x] 4.1 Add `PyJWT[cryptography]>=2.4` to `backend/pyproject.toml` dependencies and run `uv sync` to update `backend/uv.lock`. This supports the design decision: Stateless JWT validation in FastAPI (JWKS, no introspection) — no per-request network call to Keycloak. Verify: `uv run python -c "from jwt import PyJWKClient; print('ok')"` from `backend/` prints `ok` and exits `0`. Satisfies requirement: FastAPI service managed by uv (PyJWT importable), PyJWT with cryptography extras is importable.

- [x] 4.2 Create `backend/app/auth.py` (design: FastAPI JWT validation (`backend/app/auth.py`); design: Stateless JWT validation in FastAPI (JWKS, no introspection)) implementing: read `KEYCLOAK_JWKS_URI` env var (default `http://keycloak:8080/auth/realms/app/protocol/openid-connect/certs`); module-level `_jwks_client = PyJWKClient(KEYCLOAK_JWKS_URI)`; `oauth2_scheme = OAuth2PasswordBearer(tokenUrl="token", auto_error=True)`; async `get_current_user(token: str = Depends(oauth2_scheme)) -> dict` that fetches signing key via `_jwks_client.get_signing_key_from_jwt(token)`, decodes with `jwt.decode(token, signing_key, algorithms=["RS256"], options={"verify_aud": False})`, returns payload dict, raises `HTTPException(status_code=401)` on any failure. Verify: `uv run python -c "from app.auth import get_current_user; print('ok')"` from `backend/` exits `0`; `curl https://localhost/api/me` without token returns HTTP 401. Satisfies requirement: FastAPI JWKS-based JWT validation.

- [x] 4.3 Create `backend/app/routers/me.py` (design: FastAPI `/api/me` endpoint (`backend/app/routers/me.py`)) with `router = APIRouter(prefix="/me", tags=["me"])` and `GET ""` endpoint that depends on `get_current_user` and returns the decoded JWT payload dict directly. Register the router in `backend/app/main.py` with `app.include_router(me_router, prefix="/api")` and export it from `backend/app/routers/__init__.py`. Verify: `curl https://localhost/api/me` with a valid Bearer token returns HTTP 200 JSON containing `sub` and `preferred_username`. Satisfies requirement: Protected /api/me endpoint, /api/me returns JWT payload for authenticated user.

- [x] 4.4 Add `Depends(get_current_user)` to all route functions in `backend/app/routers/users.py` (design: protected routes). The `/api/health` GET handler in `backend/app/main.py` MUST NOT receive the dependency. Verify: `curl https://localhost/api/users` with no token returns HTTP 401; `curl https://localhost/api/health` with no token returns HTTP 200 `{"status":"ok"}`. Satisfies requirements: /api/users routes protected by JWT, /api/health remains publicly accessible, Unauthenticated request to /api/users is rejected.

## 5. End-to-End Smoke Test

- [x] 5.1 Rebuild and bring up the full stack (`docker compose up --build`) and confirm the complete OIDC flow: browser navigates to `https://localhost/` → redirected to Keycloak login page (Authorization Code + PKCE, no implicit flow, no client secret) → after login, Vue app mounts and `GET /api/users` returns HTTP 200 with a JSON array. Verify: no 401 or 500 errors appear in the browser console; Keycloak admin console shows an active session for the logged-in user. Satisfies all oidc-auth scenarios end-to-end.
